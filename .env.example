# Allow local Prisma installation to connect to the database container. 
# The application container has its own configuration in compose.yml.
DATABASE_URL=postgresql://app:0123456789abcdef@localhost/app